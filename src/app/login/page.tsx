// import React from 'react';
// import { Form } from "@/components/ui/form";
// import { LoginFormValues, useLoginForm } from '@/components/login/hooks/useLoginForm';


const Login = () => {
    // const onSubmit = (values: LoginFormValues) => {
    //     // You can handle modal closing logic here, e.g.,
    //     console.log("Submitting from Modal:", values);
    //     // Add API call, then close the modal
    // };
    // const { form, handleSubmit } = useLoginForm(onSubmit);
    return (
        <div>
            {/* <Form {...form}>
                <form onSubmit={handleSubmit}>


                    {/* Reusable Fields Component */}
<h1>Login page</h1>

                {/* </form>
            </Form> */}
         </div> 
    );
};

export default Login;